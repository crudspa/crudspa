@if (Model is not null)
{
    @if (Portal.AllowSignIn == true)
    {
        <Modal Model="Model"
               Width="26em"
               TopMargin="3em">
            <Content>
                @switch (Model.State)
                {
                    case SignInEmailTfaModel.States.EnterUsername:

                        <div class="c-padding-small-hor c-nowrap">
                            <div class="star">
                                <h2>Sign In</h2>
                            </div>
                        </div>

                        <form @onsubmit="Model.FindAccount"
                              @onsubmit:preventDefault="true">

                            <div class="c-padding-small-vert">

                                <div class="c-wrap">
                                    <label class="c-field medium-large">
                                        <div class="label">Username / Email</div>
                                        <input type="text"
                                               class="c-input"
                                               maxlength="75"
                                               autocomplete="username"
                                               @bind="Model.Username"
                                               @bind:event="oninput" />
                                    </label>
                                    <div class="c-stack">
                                        <label class="c-field c-checkbox tight-top tight-bottom">
                                            <input type="checkbox"
                                                   @bind="Model.RememberMe" />
                                            <div class="label">Remember username</div>
                                        </label>
                                        <label class="c-field c-checkbox tight-top">
                                            <input type="checkbox"
                                                   @bind="Model.KeepMeSignedIn" />
                                            <div class="label">Keep me signed in</div>
                                        </label>
                                    </div>
                                </div>
                            </div>

                            <div class="well center">
                                <div class="buttons">
                                    <button type="submit">
                                        <div class="c-button large save">
                                            <div class="text">Continue</div>
                                        </div>
                                    </button>
                                    <button @onclick="Model.Hide"
                                            type="button">
                                        <div class="c-button large cancel">
                                            <div class="text">Cancel</div>
                                        </div>
                                    </button>
                                </div>
                            </div>

                        </form>
                        break;

                    case SignInEmailTfaModel.States.EnterPassword:

                        <div class="c-padding-small-hor c-nowrap">
                            <div class="star">
                                <h2>Enter Password</h2>
                            </div>
                        </div>

                        <form @onsubmit="Model.CheckCredentials"
                              @onsubmit:preventDefault="true">

                            <div class="c-padding-small-vert">
                                <div class="c-wrap">
                                    <label class="c-field medium-large">
                                        <div class="label">Username / Email</div>
                                        <input type="text"
                                               class="c-input"
                                               value="@Model.Username"
                                               autocomplete="username"
                                               disabled />
                                    </label>
                                    <label class="c-field medium-large">
                                        <div class="label">Password</div>
                                        <input type="password"
                                               class="c-input"
                                               autocomplete="current-password"
                                               @bind="Model.Password"
                                               @bind:event="oninput" />
                                    </label>
                                </div>
                            </div>

                            <div class="well center">
                                <div class="buttons">
                                    <button type="submit">
                                        <div class="c-button large save">
                                            <div class="text">Continue</div>
                                        </div>
                                    </button>
                                    <button @onclick="Model.Hide"
                                            type="button">
                                        <div class="c-button large cancel">
                                            <div class="text">Cancel</div>
                                        </div>
                                    </button>
                                </div>
                            </div>

                        </form>

                        <div class="links">
                            <div class="step">
                                <a @onclick="Model.ShowResetPassword">Forgot your password?</a>
                            </div>
                            <div class="step">
                                <a @onclick="Model.ShowEnterCode">Enter one-time access code</a>
                            </div>
                            <div class="step">
                                <a @onclick="Model.ShowEnterCredentials">Use a different account</a>
                            </div>
                        </div>

                        break;

                    case SignInEmailTfaModel.States.EnterCode:

                        <div class="c-padding-small-hor">
                            <h2>Enter Code</h2>
                        </div>

                        <div class="c-padding-small-hor">
                            <p>Enter the one-time access code from your email inbox.</p>
                        </div>

                        <form @onsubmit="Model.EnterCode"
                              @onsubmit:preventDefault="true">

                            <div class="c-wrap c-padding-small-vert">

                                <label class="c-field medium-large">
                                    <div class="label">Username / Email</div>
                                    <input type="text"
                                           class="c-input"
                                           maxlength="75"
                                           autocomplete="username"
                                           @bind="Model.Username"
                                           @bind:event="oninput" />
                                </label>
                                <label class="c-field medium-large">
                                    <div class="label">Access Code</div>
                                    <input class="c-input"
                                           maxlength="6"
                                           inputmode="numeric"
                                           autocomplete="one-time-code"
                                           @bind="Model.AccessCode"
                                           @bind:event="oninput" />
                                </label>
                            </div>
                            <div class="c-stack">
                                <label class="c-field c-checkbox tight-top tight-bottom">
                                    <input type="checkbox"
                                           @bind="Model.RememberMe" />
                                    <div class="label">Remember username</div>
                                </label>
                                <label class="c-field c-checkbox tight-top">
                                    <input type="checkbox"
                                           @bind="Model.KeepMeSignedIn" />
                                    <div class="label">Stay signed in on this device</div>
                                </label>
                            </div>

                            <div class="well center">
                                <div class="buttons">
                                    <button type="submit">
                                        <div class="c-button large save">
                                            <div class="text">Enter Code</div>
                                        </div>
                                    </button>
                                    <button @onclick="Model.Hide"
                                            type="button">
                                        <div class="c-button large cancel">
                                            <div class="text">Cancel</div>
                                        </div>
                                    </button>
                                </div>
                            </div>
                        </form>

                        <div class="links">
                            <div class="step">
                                <a @onclick="Model.ShowResetPassword">Send another code</a>
                            </div>
                            <div class="step">
                                <a @onclick="Model.ShowEnterCredentials">Log in</a>
                            </div>
                        </div>

                        break;

                    case SignInEmailTfaModel.States.ResetPassword:

                        <div class="c-padding-small-hor">
                            <h2>Get an Access Code</h2>
                        </div>

                        <div class="c-padding-small-hor">
                            <p>Enter your username or email and we'll send you a one-time access code.</p>
                        </div>

                        <form @onsubmit="Model.ResetPassword"
                              @onsubmit:preventDefault="true">

                            <div class="c-wrap c-padding-small-vert">
                                <label class="c-field medium-large">
                                    <div class="label">Username / Email</div>
                                    <input type="text"
                                           class="c-input"
                                           maxlength="75"
                                           autocomplete="username"
                                           @bind="Model.Username"
                                           @bind:event="oninput" />
                                </label>
                            </div>

                            <div class="well center">
                                <div class="buttons">
                                    <button type="submit">
                                        <div class="c-button large create">
                                            <div class="text">Send Access Code</div>
                                        </div>
                                    </button>
                                    <button @onclick="Model.Hide"
                                            type="button">
                                        <div class="c-button large cancel">
                                            <div class="text">Cancel</div>
                                        </div>
                                    </button>
                                </div>
                            </div>
                        </form>

                        <div class="links">
                            <div class="step">
                                Already have a code? <a @onclick="Model.ShowEnterCode">Enter code</a>
                            </div>
                            <div class="step">
                                <a @onclick="Model.ShowEnterCredentials">Return to sign in</a>
                            </div>
                        </div>

                        break;

                    case SignInEmailTfaModel.States.ChangePassword:

                        <div class="c-padding-small-hor">
                            <h2>Change Password</h2>
                        </div>
                        <div class="c-padding-small-hor">
                            <p>Passwords must be at least 12 characters long.</p>
                        </div>

                        <form @onsubmit="Model.ChangePassword"
                              @onsubmit:preventDefault="true">

                            <div class="c-wrap c-padding-small-vert">
                                <label class="c-field medium-large">
                                    <div class="label">New Password</div>
                                    <input type="password"
                                           class="c-input"
                                           autocomplete="new-password"
                                           @bind="Model.NewPassword"
                                           @bind:event="oninput" />
                                </label>
                                <label class="c-field medium-large">
                                    <div class="label">Confirm</div>
                                    <input type="password"
                                           class="c-input"
                                           autocomplete="new-password"
                                           @bind="Model.ConfirmPassword"
                                           @bind:event="oninput" />
                                </label>
                            </div>
                            <div class="well center">
                                <div class="buttons">
                                    <button type="submit">
                                        <div class="c-button large save">
                                            <div class="text">Save</div>
                                        </div>
                                    </button>
                                    @if (!Model.ResetOnEntry)
                                    {
                                        <button @onclick="Model.Hide"
                                                type="button">
                                            <div class="c-button large cancel">
                                                <div class="text">Close</div>
                                            </div>
                                        </button>
                                    }
                                </div>
                            </div>

                        </form>

                        break;

                    case SignInEmailTfaModel.States.SignOut:

                        <div class="c-padding-small-hor">
                            <h2>Sign Out</h2>
                        </div>

                        <p class="c-padding-small-vert c-centered">
                            Are you sure you want to sign out?
                        </p>

                        <div class="well center">
                            <div class="buttons">
                                <button @onclick="Model.SignOut">
                                    <div class="c-button large destroy">
                                        <div class="text">Sign Out</div>
                                    </div>
                                </button>
                                <button @onclick="Model.Hide"
                                        type="button">
                                    <div class="c-button large cancel">
                                        <div class="text">Cancel</div>
                                    </div>
                                </button>
                            </div>
                        </div>

                        break;
                }
            </Content>
        </Modal>
    }

    @if (Portal.AllowSignIn == true || LeftPanel is not null || MenuItems is not null)
    {
        <div class="auth-bar">
            <div class="c-padding-small-hor c-nowrap">
                @if (LeftPanel is not null)
                {
                    @LeftPanel
                }
                @if (!SessionState.IsSignedIn)
                {
                    <div class="signed-out">
                        @if (Portal.AllowSignIn == true)
                        {
                            <button @onclick="() => Model.ShowEnterCredentials()">
                                <div class="c-button">
                                    <div class="text">Sign In</div>
                                </div>
                            </button>
                        }
                    </div>
                }
                else
                {
                    <div class="signed-in">
                        <div class="contact-name">
                            @(SessionState.Session.User?.Contact.Name)
                        </div>
                        <ContextMenu ButtonStyle="ContextMenu.ButtonStyles.User">
                            <MenuItem Type="MenuItem.Types.Custom"
                                      Text="Account Settings"
                                      Icon="c-icon-cog2"
                                      Clicked="() => Model.ShowAccountSettings()" />
                            @if (MenuItems is not null)
                            {
                                @MenuItems
                            }
                            @if (SessionState.Session.ServerAuthenticated != true)
                            {
                                <MenuItem Type="MenuItem.Types.Custom"
                                          Text="Change Password"
                                          Icon="c-icon-lock"
                                          Clicked="() => Model.ShowChangePassword()" />
                            }
                            <MenuItem Type="MenuItem.Types.Custom"
                                      Text="Sign Out"
                                      Icon="c-icon-exit"
                                      Clicked="() => Model.ShowSignOut()" />
                        </ContextMenu>
                    </div>
                }
            </div>
        </div>
    }
    else
    {
        <div class="auth-bar placeholder"
             aria-hidden="true">
            <span class="c-icon-menu"></span>
        </div>
    }
}