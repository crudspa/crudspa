<Waiter Model="Model">
    <Filters>
        <Fields>
            <Field Label="Search"
                   Size="Field.Sizes.Medium">
                <SearchTextBox @bind-Value="Model.Search.Text" />
            </Field>
            <Sorter Sorts="Model.Sorts"
                    @bind-Selected="Model.Search.Sort.Field"
                    @bind-Ascending="Model.Search.Sort.Ascending" />
        </Fields>
        <Buttons>
            <ButtonAdd EntityName="License"
                       Clicked="AddNew" />
            <ContextMenu ButtonStyle="ContextMenu.ButtonStyles.Create">
                <MenuItem Type="MenuItem.Types.Reset"
                          Clicked="() => Model.Reset()" />
                <MenuItem Type="MenuItem.Types.Refresh"
                          Clicked="() => Model.Refresh()" />
            </ContextMenu>
        </Buttons>
    </Filters>
</Waiter>
@if (Model.Cards.IsEmpty())
{
    <NoRecords EntityName="licenses" />
}
else
{
    @foreach (var card in Model.Cards)
    {
        <Card Model="card"
              Title="@card.Entity.Name"
              SupportsDelete="true"
              DeleteRequested="() => Model.Delete(card.Entity.Id)">
            <Values>
                <Field Size="Field.Sizes.Unspecified">
                    <LabeledHtml Label="Description"
                                 HtmlValue="@card.Entity.Description" />
                </Field>
            </Values>
            <PaneLinks>
                <PaneLink Href="@($"{Path}/license-{card.Entity.Id:D}?pane=districts")"
                          Text="Districts"
                          Count="card.Entity.DistrictLicenseCount" />
                <PaneLink Href="@($"{Path}/license-{card.Entity.Id:D}?pane=units")"
                          Text="Units"
                          Count="card.Entity.UnitLicenseCount" />
                <PaneLink Href="@($"{Path}/license-{card.Entity.Id:D}?pane=segments")"
                          Text="Segments"
                          Count="card.Entity.SegmentLicenseCount" />
                <PaneLink Href="@($"{Path}/license-{card.Entity.Id:D}?pane=assessments")"
                          Text="Assessments"
                          Count="card.Entity.AssessmentLicenseCount" />
                <PaneLink Href="@($"{Path}/license-{card.Entity.Id:D}?pane=blogs")"
                          Text="Blogs"
                          Count="card.Entity.BlogLicenseCount" />
                <PaneLink Href="@($"{Path}/license-{card.Entity.Id:D}?pane=forums")"
                          Text="Forums"
                          Count="card.Entity.ForumLicenseCount" />
                <PaneLink Href="@($"{Path}/license-{card.Entity.Id:D}?pane=campaigns")"
                          Text="Campaigns"
                          Count="card.Entity.CampaignLicenseCount" />
                <PaneLink Href="@($"{Path}/license-{card.Entity.Id:D}?pane=tracks")"
                          Text="Tracks"
                          Count="card.Entity.TrackLicenseCount" />
                <PaneLink Href="@($"{Path}/license-{card.Entity.Id:D}?pane=surveys")"
                          Text="Surveys"
                          Count="card.Entity.SurveyLicenseCount" />
            </PaneLinks>
            <Buttons>
                <ButtonView Href="@($"{Path}/license-{card.Entity.Id:D}")" />
                <ContextMenu ButtonStyle="ContextMenu.ButtonStyles.View">
                    <MenuItem Type="MenuItem.Types.Delete"
                              Clicked="() => Model.ShowConfirmation(card.Entity.Id)" />
                </ContextMenu>
            </Buttons>
        </Card>
    }

    <Pager Paged="Model.Search.Paged"
           PageNumberChanged="Model.HandlePageNumberChanged" />
}