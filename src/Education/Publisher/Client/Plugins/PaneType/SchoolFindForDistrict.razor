<Waiter Model="Model">
    <Filters>
        <Fields>
            <Field Label="Search"
                   Size="Field.Sizes.Medium">
                <SearchTextBox @bind-Value="Model.Search.Text" />
            </Field>
            <Field Label="Community"
                   Size="Field.Sizes.Medium">
                <MultiSelect LookupValues="Model.CommunityNames"
                             @bind-SelectedValues="Model.Search.Communities" />
            </Field>
            <Sorter Sorts="Model.Sorts"
                    @bind-Selected="Model.Search.Sort.Field"
                    @bind-Ascending="Model.Search.Sort.Ascending" />
        </Fields>
        <Buttons>
            <ButtonAdd EntityName="School"
                       Clicked="AddNew" />
            <ContextMenu ButtonStyle="ContextMenu.ButtonStyles.Create">
                <MenuItem Type="MenuItem.Types.Reset"
                          Clicked="() => Model.Reset()" />
                <MenuItem Type="MenuItem.Types.Refresh"
                          Clicked="() => Model.Refresh()" />
            </ContextMenu>
        </Buttons>
    </Filters>
</Waiter>
@if (Model.Cards.IsEmpty())
{
    <NoRecords EntityName="schools" />
}
else
{
    @foreach (var card in Model.Cards)
    {
        <Card Model="card"
              Title="@card.Entity.Name"
              SupportsDelete="true"
              DeleteRequested="() => Model.Delete(card.Entity.Id)">
            <Values>
                <OrganizationDisplay Organization="card.Entity.Organization">
                    <UsaPostalDisplay UsaPostal="card.Entity.UsaPostal">
                        <Field Size="Field.Sizes.Medium">
                            <Labeled Width="8em"
                                     Label="Key"
                                     StringValue="@card.Entity.Key" />
                            <Labeled Width="8em"
                                     Label="Community"
                                     StringValue="@card.Entity.CommunityName" />
                        </Field>
                    </UsaPostalDisplay>
                </OrganizationDisplay>
            </Values>
            <PaneLinks>
                <PaneLink Href="@($"{Path}/school-{card.Entity.Id:D}?pane=contacts")"
                          Text="Contacts"
                          Count="card.Entity.SchoolContactCount" />
            </PaneLinks>
            <Buttons>
                <ButtonView Href="@($"{Path}/school-{card.Entity.Id:D}")" />
                <ContextMenu ButtonStyle="ContextMenu.ButtonStyles.View">
                    <MenuItem Type="MenuItem.Types.Delete"
                              Clicked="() => Model.ShowConfirmation(card.Entity.Id)" />
                </ContextMenu>
            </Buttons>
        </Card>
    }

    <Pager Paged="Model.Search.Paged"
           PageNumberChanged="Model.HandlePageNumberChanged" />
}