<List Model="Model"
      T="MembershipModel"
      EntityName="Membership"
      UrlPrefix="@($"{Path}/sms-membership")"
      ShowAdd="false"
      ShowDelete="false">
    <ReadView>
        <Field Size="Field.Sizes.Unspecified">
            <LabeledHtml Label="Description"
                         HtmlValue="@context.Membership.Description" />
            <Labeled Width="6em"
                     Label="Supports Opt Out"
                     BooleanValue="@context.Membership.SupportsOptOut" />
        </Field>
    </ReadView>
    <PaneLinks>
        <PaneLink Href="@($"{Path}/sms-membership-{context.Membership.Id:D}?pane=messages")"
                  Text="Messages" />
        <PaneLink Href="@($"{Path}/sms-membership-{context.Membership.Id:D}?pane=templates")"
                  Text="Templates" />
    </PaneLinks>
</List>