<Waiter Model="Model">
    <Filters>
        <Fields>
            <Field Label="Search"
                   Size="Field.Sizes.Medium">
                <SearchTextBox @bind-Value="Model.Search.Text" />
            </Field>
            <Field Label="Status Changed"
                   Size="Field.Sizes.Medium">
                <DateFilter Range="Model.Search.StatusChangedRange" />
            </Field>
            <Sorter Sorts="Model.Sorts"
                    @bind-Selected="Model.Search.Sort.Field"
                    @bind-Ascending="Model.Search.Sort.Ascending" />
        </Fields>
        <Buttons>
            <ButtonAdd EntityName="Text Preference"
                       Clicked="AddNew" />
            <ContextMenu ButtonStyle="ContextMenu.ButtonStyles.Create">
                <MenuItem Type="MenuItem.Types.Reset"
                          Clicked="() => Model.Reset()" />
                <MenuItem Type="MenuItem.Types.Refresh"
                          Clicked="() => Model.Refresh()" />
            </ContextMenu>
        </Buttons>
    </Filters>
</Waiter>
@if (Model.Cards.IsEmpty())
{
    <NoRecords EntityName="text preferences" />
}
else
{
    @foreach (var card in Model.Cards)
    {
        <Card Model="card"
              Title="@card.Entity.Number"
              SupportsDelete="true"
              DeleteRequested="() => Model.Delete(card.Entity.Id)">
            <Values>
                <Field Size="Field.Sizes.Unspecified">
                    <Labeled Width="8em"
                             Label="Number"
                             StringValue="@card.Entity.Number" />
                    <Labeled Width="8em"
                             Label="Status"
                             StringValue="@card.Entity.Status.GetLabel()" />
                    <Labeled Width="8em"
                             Label="Source"
                             StringValue="@card.Entity.Source.GetLabel()" />
                    <Labeled Width="8em"
                             Label="Status Changed"
                             DateTimeOffsetValue="@card.Entity.StatusChanged" />
                    <Labeled Width="8em"
                             Label="Notes"
                             StringValue="@card.Entity.Notes" />
                </Field>
            </Values>
            <Buttons>
                <ButtonView Href="@($"{Path}/sms-preference-{card.Entity.Id:D}")" />
                <ContextMenu ButtonStyle="ContextMenu.ButtonStyles.View">
                    <MenuItem Type="MenuItem.Types.Delete"
                              Clicked="() => Model.ShowConfirmation(card.Entity.Id)" />
                </ContextMenu>
            </Buttons>
        </Card>
    }

    <Pager Paged="Model.Search.Paged"
           PageNumberChanged="Model.HandlePageNumberChanged" />
}